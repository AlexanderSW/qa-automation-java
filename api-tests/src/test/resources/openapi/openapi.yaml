# api-tests/src/test/resources/openapi/openapi.yaml
openapi: 3.0.3
info:
  title: Demo API
  version: 1.0.0
servers:
  - url: http://178.150.3.212
paths:
  /health:
    get:
      summary: Health check
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  status: { type: string }
